package com.hoho.android.usbserial.core;

public enum DongleNoti {
    NONE,
    AT_MODE,
    BLE_CONNECT_CHECK,

    MASTER_CHECK,
    BLE_SCAN_START,
    BLE_SCAN_FINISHED,
    BLE_CONNECTING,
    BLE_CONNECTED,
    BLE_CONNECT_FAIL,
    BLE_DISCONNECTED,
    DT_MODE
}
